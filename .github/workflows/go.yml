# This workflow will build a golang project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-go

name: Build Siege Listener

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go-version: [1.21.x]
        os: [ubuntu-latest, ubuntu-20.04, redhat-7, debian-latest]
        include:
          - os: redhat-7
            distro: 'rhel/7'
            install_cmd: "yum install -y golang libpcap-devel"
          - os: redhat
            version: "9"
            container: registry.access.redhat.com/ubi9/ubi:latest
            install_cmd: "yum install -y golang libpcap-devel"
          - os: debian-latest
            distro: 'debian/buster'
            install_cmd: "apt-get update && apt-get install -y golang-go libpcap-dev"
          - os: ubuntu-latest
            distro: 'ubuntu/latest'
            install_cmd: "apt-get update && apt-get install -y golang-go libpcap-dev"
          - os: ubuntu-20.04
            distro: 'ubuntu/20.04'
            install_cmd: "apt-get update && apt-get install -y golang-go libpcap-dev"

    steps:
    - uses: actions/checkout@v3

    - name: Set up Go ${{ matrix.go-version }}
      uses: actions/setup-go@v4
      with:
        go-version: ${{ matrix.go-version }}

    - name: Build for ${{ matrix.distro }}
      run: |
        echo "Building for ${GITHUB_WORKFLOW} on ${{ matrix.distro }}"
        go build -v ./...

    - name: Test
      run: go test -v ./...
